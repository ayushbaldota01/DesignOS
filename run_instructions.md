# Running the DesignOS Flask Server

To start the local Flask server for DesignOS, run the following command in the project root:

```bash
venv2\Scripts\python.exe app.py
```

The server runs on `http://127.0.0.1:5000` (or `http://localhost:5000`).
